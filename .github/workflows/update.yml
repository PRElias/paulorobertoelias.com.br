name: Update environment

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: update gems
      run: bundle update --bundler

    - name: install
      run: gem install bundler:1.17.1
        
